import { mapBreakpoints } from "lib/util";
import { breakpoints as shaktiBreakpoints } from "shakti-lib";

/**
 * Theme breakpoints for media queries.
 */
const breakpoints = mapBreakpoints(shaktiBreakpoints);

export default breakpoints;

/**
 * Append suffix to a string.
 * @param {string} string base input string.
 * @param {string} suffix suffix to append to end of string.
 * @returns
 */
const appendSuffix = (string: string, suffix: string) => `${string}${suffix}`;

export default appendSuffix;
